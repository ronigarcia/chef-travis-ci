---
driver:
  name: vagrant

provisioner:
  name: chef_zero

platforms:
  - name: ubuntu-18.04
  - name: ubuntu-14.04
  - name: centos-7

verifier:
  name: inspec
  
suites:
  - name: default
    run_list:
      - recipe[install-apache::default]
    verifier:
      inspec_tests:
        - test/integration/default
    attributes:
